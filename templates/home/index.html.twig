{% extends 'base.html.twig' %}

{% block body %}

    <h1>Home</h1>

    {% for room in rooms %}
        <p>{{ room.description }}</p>
        <p>{{ room.category.name }}</p>
    {% endfor %}

{% endblock %}