{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button type="submit" class="btn btn-primary mt-3" formnovalidate>Save</button>
    {{ form_end(form) }}

{% endblock %}